const removeFromArray = function(array, num) {
    return array.splice(num, 1);
};

//so this num is the number that should be removed from the array.  
//For example, if it's 3 then space 3 will be taken out.
//use .foreach and .push

// Do not edit below this line
module.exports = removeFromArray;
